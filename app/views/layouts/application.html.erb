<!DOCTYPE html>
<html>
  <head>
    <title>PostV3</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application" %>
  </head>
  <body>
      <div id="modal">
        <%= render "devise/sessions/new", show_modal: false %>
      </div>
      <%= render 'shared/nav'%>
      <%= yield %>
  </body>
</html>
